// -------------------------
// Base
// -------------------------

*,
*:before,
*:after {
	box-sizing: border-box;
}

html,
body {
	-moz-osx-font-smoothing: grayscale;
	-webkit-font-smoothing: antialiased;
	color: content-color;
	fs: small-font-size;

	+above('medium') {
		fs: medium-font-size;
	}

	+above('large') {
		fs: base-font-size;
	}

}

h1,
h2,
h3,
h4,
h5,
h6 {
	color: heading-color;
}

a {
	color: link-color;
}

img {
	max-width: 100%;
}
